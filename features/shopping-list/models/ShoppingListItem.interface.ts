/** Una fila de la lista general tal como la muestra la pantalla. */
export interface ShoppingListItem {
  id: string;
  productName: string;
  quantity: number;
  sizeLabel: string;
  variantId: string;
}
